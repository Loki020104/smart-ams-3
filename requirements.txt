flask==3.0.0
flask-cors==4.0.0
# dlib-bin provides pre-compiled dlib wheels — avoids the 8GB+ memory
# spike that occurs when compiling dlib from source on Render's free tier.
dlib-bin>=19.24.0

# face-recognition was previously used but has been removed
# in favor of direct dlib usage. If you still want the
# convenience wrapper you can install it alongside dlib:
# face-recognition==1.3.0

opencv-python-headless==4.8.1.78
numpy>=1.26.0
Pillow>=10.1.0
requests>=2.31.0
python-dotenv==1.0.0
cryptography>=41.0.0
qrcode>=7.4.0
pycryptodome>=3.19.0
supabase>=0.6.0
user-agent>=0.1.10
geopy>=2.3.0