# Eye Blinking Liveness Detection - Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    SmartAMS Attendance System                   │
│                  with Eye Blinking Liveness                     │
└─────────────────────────────────────────────────────────────────┘

                           Student
                              ↓
                    Capture Face Photo
                              ↓
                    ┌─────────────────┐
                    │   Web Interface │  or  ┌──────────────┐
                    │   (index.html)  │      │ Python API   │
                    └────────┬────────┘      └──────┬───────┘
                             ↓                      ↓
                    ┌───────────────────────────────────┐
                    │   Flask Backend (backend.py)      │
                    │   POST /api/verify                │
                    └────────┬────────────────────────────┘
                             ↓
          ┌──────────────────────────────────────────────┐
          │      1. LIVENESS CHECK (NEW!)               │
          │  ┌────────────────────────────────────┐    │
          │  │   Load dlib Components:            │    │
          │  │   • Frontal Face Detector          │    │
          │  │   • Shape Predictor (68 landmarks) │    │
          │  └────────────────────┬───────────────┘    │
          │                       ↓                    │
          │  ┌────────────────────────────────────┐    │
          │  │   Detect Face in Image             │    │
          │  └────────────────────┬───────────────┘    │
          │                       ↓                    │
          │  ┌────────────────────────────────────┐    │
          │  │   Extract Eye Landmarks            │    │
          │  │   • Left eye: indices 36-41        │    │
          │  │   • Right eye: indices 42-47       │    │
          │  └────────────────────┬───────────────┘    │
          │                       ↓                    │
          │  ┌────────────────────────────────────┐    │
          │  │   Calculate Eye Aspect Ratio:      │    │
          │  │   EAR = (L + R) / (2 × H)          │    │
          │  └────────────────────┬───────────────┘    │
          │                       ↓                    │
          │          Is EAR ≥ 0.1 threshold?          │
          │            ↙              ↘                │
          │          YES              NO               │
          │           ↓               ↓                │
          │   ✓ PASS              ✗ FAIL              │
          │   Proceed             REJECT              │
          │                   (Fake Image)            │
          └──────────────────────────────────────────────┘
                             ↓
          ┌──────────────────────────────────────────────┐
          │   2. FACE ENCODING                         │
          │   (If liveness check PASSED)               │
          │   • Detect faces using face_recognition    │
          │   • Generate 128-D encoding (ResNet)       │
          └────────────────┬─────────────────────────────┘
                           ↓
          ┌──────────────────────────────────────────────┐
          │   3. FACE MATCHING                         │
          │   • Load registered encodings from disk    │
          │   • Calculate distance to all registered   │
          │   • Find best match                        │
          │   • Check if distance ≤ tolerance (0.5)    │
          └────────────────┬─────────────────────────────┘
                           ↓
          ┌──────────────────────────────────────────────┐
          │   4. MARK ATTENDANCE                       │
          │   (If face matches)                        │
          │   • Append to attendance.csv (local)       │
          │   • Insert to Supabase (if configured)     │
          └────────────────┬─────────────────────────────┘
                           ↓
                    JSON Response
                           ↓
                   Display Result to Student
```

## Liveness Detection Detail

```
                    IMAGE LOADED
                         ↓
              ┌──────────────────────┐
              │   dlib Preprocessing  │
              │ (Convert to grayscale)│
              └──────────┬───────────┘
                         ↓
         ┌────────────────────────────────┐
         │  Frontal Face Detector         │
         │  detector(gray, upsampling=0)  │
         └────────────┬───────────────────┘
                      ↓
        ┌─────────────────────────────┐
        │  Found faces?               │
        ├─────────────────────────────┤
        │  0 faces → FAIL             │
        │  1 face  → Process          │
        │  >1 face → FAIL (multiple)  │
        └────────┬────────────────────┘
                 ↓
    ┌────────────────────────────────┐
    │ Shape Predictor                │
    │ sp(gray, face)                 │
    │ Returns 68 landmarks           │
    └────────┬───────────────────────┘
             ↓
┌────────────────────────────────────────┐
│  Extract Eye Points from Landmarks     │
│                                        │
│  Left Eye (Landmark 36-41):            │
│    36: left corner                     │
│    37: top-left                        │
│    38: top-right                       │
│    39: right corner                    │
│    40: bottom-right                    │
│    41: bottom-left                     │
│                                        │
│  Right Eye (Landmark 42-47): Similar   │
└────────┬───────────────────────────────┘
         ↓
┌─────────────────────────────────────────┐
│ Calculate Eye Aspect Ratio (EAR)        │
│                                         │
│ Left Eye:                               │
│  p1=(36), p2=(37), p3=(38)              │
│  p4=(39), p5=(40), p6=(41)              │
│                                         │
│   dist_vertical_1 = ||p2 - p6||        │
│   dist_vertical_2 = ||p3 - p5||        │
│   dist_horizontal = ||p1 - p4||        │
│                                         │
│   EAR_left = (dist_v1 + dist_v2) /     │
│              (2 × dist_h)               │
│                                         │
│ Right Eye: Same calculation             │
│                                         │
│ AVERAGE_EAR = (EAR_left + EAR_right) / 2│
└────────┬───────────────────────────────┘
         ↓
┌─────────────────────────────────────────┐
│ Check Threshold: AVERAGE_EAR ≥ 0.1?     │
├─────────────────────────────────────────┤
│                                         │
│  EAR = 0.0 - 0.05   → EYES CLOSED ✗   │
│  EAR = 0.05 - 0.10  → BORDERLINE      │
│  EAR = 0.10 - 0.20  → EYES OPEN ✓    │
│  EAR = 0.20+        → VERY OPEN       │
│                                         │
└────────┬───────────────────────────────┘
         ↓
    ┌─────────────┐
    │ PASS or FAIL│ → Return Result
    └─────────────┘
```

## Eye Aspect Ratio Visualization

### Eyes OPEN (EAR > 0.1) ✓ LIVENESS CHECK PASSES

```
LEFT EYE:                RIGHT EYE:
p1 ──── p2 ──── p3       p1 ──── p2 ──── p3
 │      ║      │         │      ║      │
 │      ║      │   ║─┤   │      ║      │
 │      ║      │         │      ║      │
p6 ──── p5 ──── p4       p6 ──── p5 ──── p4

High vertical distance (║)  = Large EAR
Horizontal distance (─)     = Base

Formula: EAR = (top + top) / (2 × horizontal)
         = (large + large) / (2 × base)
         = 0.40-0.50 (PASS if > 0.1)
```

### Eyes CLOSED (EAR < 0.1) ✗ LIVENESS CHECK FAILS

```
LEFT EYE:                RIGHT EYE:
p1 ──── p2 ──── p3       p1 ──── p2 ──── p3
 │ ─ ─ ─ ─ ─ ─ ─ │        │ ─ ─ ─ ─ ─ ─ ─ │
 │      (closed) │       │      (closed) │
p6 ──── p5 ──── p4       p6 ──── p5 ──── p4

Low vertical distance    = Small EAR
Horizontal distance     = Similar base

Formula: EAR = (small + small) / (2 × base)
         = 0.01-0.05 (FAIL if < 0.1)
```

## Data Flow Diagram

```
REGISTRATION                          VERIFICATION
==============                        ==============

Student                               Student
   ↓                                     ↓
Capture 5                             Capture 1
Photos                                Photo
   ↓                                     ↓
┌──────────────┐                    ┌──────────────┐
│ For each     │                    │ Liveness     │
│ sample:      │                    │ Check (EAR)  │
├──────────────┤                    ├──────────────┤
│1. Liveness?  │                    │ EAR ≥ 0.1?   │
│   (Eyes open)│                    │              │
│   ↓          │                    │ YES → Continue
│  YES/NO      │                    │ NO  → REJECT │
│   ↓          │                    └──────┬───────┘
│2. Encode     │                           ↓
│   face       │                    Encode Face
│   ↓          │                    (128-D vector)
│3. Save to    │                           ↓
│   encodings  │                    Load all
│   .pkl       │                    registered
│   ↓          │                    encodings
│ STORED       │                           ↓
└──────────────┘                    Calculate
                                    distances
                                    (to all)
                                           ↓
                                    Find best
                                    match
                                           ↓
                                    Distance
                                    ≤ 0.5?
                                           ↓
                                    YES → MARK
                                    ATTENDANCE
                                           ↓
                                    VERIFIED ✓
```

## Module Dependencies

```
┌─────────────────────────────────────────────┐
│          face_recognition_with_liveness.py  │
│                    (Main Module)             │
└─────────────────────────────────────────────┘
         │
         ├─→ numpy              (Math operations)
         ├─→ PIL (Pillow)       (Image loading)
         ├─→ cv2 (OpenCV)       (Image processing)
         ├─→ face_recognition   (Face encoding)
         ├─→ dlib               (Eye detection)
         ├─→ csv                (Attendance logging)
         ├─→ pickle             (Encoding storage)
         └─→ pathlib, datetime  (File/time handling)

┌─────────────────────────────────────────────┐
│              backend.py (Flask)              │
│           (Web API Integration)             │
└─────────────────────────────────────────────┘
         │
         ├─→ face_recognition_with_liveness
         └─→ flask, flask_cors   (Web server)

┌─────────────────────────────────────────────┐
│             test_liveness.py                 │
│            (Testing & Validation)           │
└─────────────────────────────────────────────┘
         └─→ All of above
```

## API Response Flow

```
CLIENT                              SERVER
  │                                    │
  ├─ POST /api/verify ─────────────→  │
  │  {                                 │
  │    "image": "base64_data...",      │
  │    "tolerance": 0.5                │
  │  }                                 │
  │                                    ├─ 1. Decode image
  │                                    ├─ 2. Liveness check
  │                                    │    (dlib + EAR)
  │                                    ├─ 3. Face encoding
  │                                    ├─ 4. Face matching
  │                                    ├─ 5. Mark attendance
  │                                    │
  │  ← 200 OK (JSON response) ────────┤
  │  {                                 │
  │    "verified": true,               │
  │    "name": "LOKNATH",              │
  │    "confidence": 0.8045,           │
  │    "face_count": 1                 │
  │  }                                 │
  │                                    │
  ├─ Display: ✓ Verified              │
  │           LOKNATH (80.45%)         │
  │                                    │
```

## Threshold Tuning Graph

```
EAR Value
  ↑
  │
0.5 │        ╔═══════════════╗
  │        ║ VERY OPEN    ║
  │        ║ (Suspicious) ║
0.3 │   ┌────║──────────────║──────┐
  │   │ ║ NORMAL OPEN  ║    │
  │   │ ╚═══════════════╝    │
0.1 │   ├─ ─ ─ ─ ─ ─THRESHOLD─ ─ ─ ┤
  │   │         (0.1)          │
  │   │  ┌──────────────┐      │
  │   │  │ BORDERLINE   │      │
  │   │  └──────────────┘      │
0.05│   │       ╔════════╗    │
  │   │       ║ CLOSED ║    │
  │   │       ║ (FAIL) ║    │
0.0 │   └────  ╚════════╝ ───┘
  └─────────────────────────────→ Time/Angle
```

## Liveness Check Confidence Levels

```
EAR Range        | Status   | Action
─────────────────┼──────────┼──────────────────
0.00 - 0.06      | CLOSED   | ✗ REJECT
0.06 - 0.10      | WINKING  | ⚠ WARN/RETRY
0.10 - 0.15      | PASS     | ✓ ACCEPT*
0.15 - 0.25      | OPEN     | ✓ ACCEPT
0.25+            | VERY OPEN| ✓ ACCEPT
─────────────────┴──────────┴──────────────────

* = Borderline, depends on threshold setting

Recommendation: Use 0.10-0.15 range for
                optimal balance between
                security and usability
```

---

**Legend**:
- ✓ = Success / Pass
- ✗ = Fail / Reject
- ⚠ = Warning / Retry
- → = Process flow
- ║ = Data flow
- ─ = Measurement
- ═ = Data structure

---

Last Updated: 2026-03-02
